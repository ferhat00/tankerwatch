# TankerWatch Environment Variables
# Copy this file to .env and fill in your values

# Optional: Mapbox token (leave empty to use open-street-map style which needs no token)
MAPBOX_TOKEN=

# Optional: override database path
# DATABASE_PATH=data/tankerwatch.db

# Optional: override scraper interval
# SCRAPER_INTERVAL_MINUTES=15
